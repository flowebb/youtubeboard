# YouTube Board (TrendBoard)

YouTube 채널 랭킹을 조회할 수 있는 대시보드 애플리케이션입니다.

## 기능

- 📊 YouTube 채널 랭킹 조회
- 🔍 채널명으로 검색
- 📅 날짜별 랭킹 조회
- 📈 순위 변화 추적
- 🎨 다크 테마 UI

## 시작하기

### 1. 의존성 설치

```bash
npm install
```

### 2. YouTube API 키 설정

1. [Google Cloud Console](https://console.cloud.google.com/)에서 프로젝트 생성
2. YouTube Data API v3 활성화
3. API 키 생성
4. 프로젝트 루트에 `.env` 파일 생성:

```env
VITE_YOUTUBE_API_KEY=your_api_key_here
```

**참고:** API 키가 없어도 샘플 데이터로 앱을 실행할 수 있습니다.

### 3. 개발 서버 실행

```bash
npm run dev
```

브라우저에서 `http://localhost:5173` (또는 표시된 포트)로 접속하세요.

## 프로젝트 구조

```
src/
├── components/       # 재사용 가능한 컴포넌트
│   ├── Layout.jsx   # 레이아웃 (사이드바, 헤더)
│   └── Layout.css
├── pages/           # 페이지 컴포넌트
│   ├── Ranking.jsx  # 랭킹 페이지
│   ├── Ranking.css
│   ├── Insights.jsx # 인사이트 페이지
│   └── Insights.css
├── services/        # API 서비스
│   └── youtubeApi.js # YouTube API 연동
├── App.jsx          # 메인 앱 컴포넌트
└── main.jsx         # 앱 진입점
```

## 사용 기술

- **React** - UI 라이브러리
- **Vite** - 빌드 도구
- **React Router** - 라우팅
- **YouTube Data API v3** - YouTube 데이터 조회

## 빌드

프로덕션 빌드:

```bash
npm run build
```

빌드 미리보기:

```bash
npm run preview
```

## 라이선스

MIT
